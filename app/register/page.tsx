import { RegisterForm } from "@/components/auth/create-account"

export default function LoginPage() {
  return (
    <div className="min-h-screen w-full flex items-center justify-center bg-[#dbeafe] px-4 sm:px-0">
      <RegisterForm />
    </div>
  )
}
